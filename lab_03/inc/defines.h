#pragma once

typedef enum
{
    SUCCESS = 0,
    WRONG_POS_ERROR,
    NULLPTR_ERROR,
    ALLOC_ERROR,
    ELEMENT_EXIST_ERROR,
    NAN_ERROR,
    REPLACE_ERROR
} ERRORS;

typedef enum
{
    CREATE_MATRIX,
    CREATE_VECTOR,
    INPUT_MATRIX,
    INPUT_VECTOR,
    MULTIPLICATION,
    PRINT_MATRIX,
    PRINT_VECTOR,
    COMPARISON,
    EXIT
} OPCODES;
