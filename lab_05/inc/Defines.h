#pragma once

typedef enum
{
    SUCCESS = 0,
    NULLPTR_ERROR,
    ALLOC_ERROR,
    NAN_ERROR,
    EMPTY_QUEUE_POP,
    QUEUE_OVERFLOW
} ERRORS;

typedef enum
{
    CHANGE_REQUESTS,
    CHANGE_TIMINGS,
    SIMULATE_ARR,
    SIMULATE_LINKED,
    COMPARE_TADS,
    PRINT_FREED,
    EXIT
} OPCODES;
